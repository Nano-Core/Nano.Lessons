services:
  api.cookies:
    image: api.cookies
    hostname: api-cookies
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.Cookies
      dockerfile: "Dockerfile.Local"
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
