services:
  api.data.identity.auth.jwt:
    image: api.data.identity.auth.jwt
    hostname: api-data-auth-jwt
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.Data.Identity.Auth.Jwt
      dockerfile: "Dockerfile.Local"
    depends_on:
      - database
    networks:
      - network

  database:
    image: mysql/mysql-server:latest
    ports:
      - 3306:3306
    networks:
      - network
    environment:
      MYSQL_ROOT_HOST: '%'
      MYSQL_ROOT_PASSWORD: myPassword_123

networks:
  network:
    name: network
    driver: bridge
