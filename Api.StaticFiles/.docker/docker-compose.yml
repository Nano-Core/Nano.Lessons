services:
  api.staticfiles:
    image: api.staticfiles
    hostname: api-staticfiles
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.StaticFiles
      dockerfile: "Dockerfile.Local"
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
