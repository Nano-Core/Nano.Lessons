services:
  console.customservice:
    image: console.customservice
    restart: on-failure
    build:
      context: ../Console.CustomService
      dockerfile: "Dockerfile.Local"
    env_file:
      - .env
    networks:
      - network

networks:
  network:
    name: network
    external: true
