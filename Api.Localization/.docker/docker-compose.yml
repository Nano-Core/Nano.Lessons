services:
  api.localization:
    image: api.localization
    hostname: api-localization
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.Localization
      dockerfile: "Dockerfile.Local"
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
