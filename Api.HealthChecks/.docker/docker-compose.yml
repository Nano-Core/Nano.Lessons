services:
  api.healthchecks:
    image: api.healthchecks
    hostname: api-healthchecks
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.HealthChecks
      dockerfile: "Dockerfile.Local"
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
