services:
  api.requesttracing:
    image: api.requesttracing
    hostname: api-requesttracing
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.RequestTracing
      dockerfile: "Dockerfile.Local"
    env_file:
      - .env
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
