services:
  console.startuptasks:
    image: console.startuptasks
    restart: on-failure
    build:
      context: ../Console.StartupTasks
      dockerfile: "Dockerfile.Local"
    env_file:
      - .env
    networks:
      - network

networks:
  network:
    name: network
    external: true
