services:
  api.customservice:
    image: api.customservice
    hostname: api-customservice
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.CustomService
      dockerfile: "Dockerfile.Local"
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
