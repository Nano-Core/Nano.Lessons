services:
  api.authorization:
    image: api.authorization
    hostname: api-authorization
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.Authorization
      dockerfile: "Dockerfile.Local"
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
