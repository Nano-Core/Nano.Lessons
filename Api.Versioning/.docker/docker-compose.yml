services:
  api.versioning:
    image: api.versioning
    hostname: api-versioning
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.Versioning
      dockerfile: "Dockerfile.Local"
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
