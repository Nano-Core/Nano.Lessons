services:
  api.data.mysql:
    image: api.data.mysql
    hostname: api-data-mysql
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.Data.MySql
      dockerfile: "Dockerfile.Local"
    depends_on:
      - database
    networks:
      - network

  database:
    image: mysql/mysql-server:latest
    ports:
      - 3306:3306
    networks:
      - network
    environment:
      MYSQL_ROOT_HOST: '%'
      MYSQL_ROOT_PASSWORD: myPassword_123

networks:
  network:
    name: network
    driver: bridge
