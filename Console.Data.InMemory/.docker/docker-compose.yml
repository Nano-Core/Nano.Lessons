services:
  console.data.inmemory:
    image: console.data.inmemory
    restart: on-failure
    build:
      context: ../Console.Data.InMemory
      dockerfile: "Dockerfile.Local"
    env_file:
      - .env
    networks:
      - network

networks:
  network:
    name: network
    external: true
