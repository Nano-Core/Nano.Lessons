services:
  api.data.mysql.mappings:
    image: api.data.mysql.mappings
    hostname: api-data-mysql-mappings
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.Data.MySql.Mappings
      dockerfile: "Dockerfile.Local"
    depends_on:
      - database
    networks:
      - network

  database:
    image: mysql/mysql-server:latest
    ports:
      - 3306:3306
    networks:
      - network
    environment:
      MYSQL_ROOT_HOST: '%'
      MYSQL_ROOT_PASSWORD: myPassword_123

networks:
  network:
    name: network
    driver: bridge
