services:
  api.metrics:
    image: api.metrics
    hostname: api-metrics
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.Metrics
      dockerfile: "Dockerfile.Local"
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
