services:
  console.exceptionhandling:
    image: console.exceptionhandling
    restart: on-failure
    build:
      context: ../Console.ExceptionHandling
      dockerfile: "Dockerfile.Local"
    env_file:
      - .env
    networks:
      - network

networks:
  network:
    name: network
    external: true
