services:
  api.responsecache:
    image: api.responsecache
    hostname: api-responsecache
    restart: on-failure
    ports:
      - 8080:8080
    build:
      context: ../Api.ResponseCache
      dockerfile: "Dockerfile.Local"
    networks:
      - network

networks:
  network:
    name: network
    driver: bridge
